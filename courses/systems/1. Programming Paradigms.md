# Programming Paradigms

## 1. Introduction to Programming Paradigms

A paradigm is a theoretical model of thought that guides scientific research and reflection. Programming refers to the set of activities involved in writing computer programs, generally understood as the writing of a software's source code.

A programming language is an abstraction of the operations performed by a computer. A language is usually associated with a dominant paradigm from which its structure was designed, although elements from several paradigms are often present within the same language. Programming consists of using a language to conform, to varying degrees, to one or more programming paradigms. A single programming language can combine features from multiple paradigms. For example, a language designed primarily around imperative constructs may also support functional or object-oriented features.

The study of multiple paradigms serves several purposes: expressing complex ideas more effectively, adapting to the requirements of different tasks, facilitating the learning of new languages, supporting informed choices among languages, and following developments in programming practice.

| Language | Creator / Origin | Purpose / Characteristics |
|---|---|---|
| Ada | U.S. Department of Defense | Designed for embedded systems and concurrency |
| Algol | ACM and GAMM | Used notation close to mathematics; aimed at machine-independent algorithm description |
| ANSI C | ANSI committee | Standardized version of C intended to ensure portability |
| APL | K. Iverson | Designed for mathematical programming using matrices and vectors |
| B | K. Thompson | Used as a simple compiler language in early Unix development |
| BASIC | J. Kemeny and T. Kurtz | Designed to be simple and accessible |
| BCPL | Derived from CPL | Simplified version of CPL |
| C | B. W. Kernighan and D. M. Ritchie | Designed for systems programming close to the machine |
| C++ | B. Stroustrup | Extended C with object-oriented features |
| C# | Microsoft | Combines features of C++ and Java for the .NET platform |
| COBOL | Short Range Committee | Designed for business applications using English-like syntax |
| Common Lisp | Committee effort | Unified various Lisp dialects |
| CPL | North American counterpart to Algol | Closer to machine operations than Algol |
| Eiffel | B. Meyer | Designed to support object-oriented design principles |
| Fortran | J. Backus | Designed for the IBM 704, emphasizing code efficiency |
| Java | J. Gosling, Sun Microsystems | Resembles C++; follows object-oriented principles more closely |
| Lisp | J. McCarthy | Designed for artificial intelligence research; uses the list as its principal data structure |
| Miranda | D. Turner | Purely functional language with fully lazy evaluation |
| ML | R. Milner | Strongly typed functional language with type inference |
| Pascal | N. Wirth | Designed for teaching purposes |
| PL/I | "3x3" committee | Intended as a universal language; became complex in practice |
| Prolog | Marseille AI group | Designed for expert systems |
| Scheme | MIT | Lisp dialect used for teaching |
| Simula | K. Nygaard and O. J. Dahl | Introduced the concept of the class for simulation purposes |
| Smalltalk | A. Kay | Early object-oriented language using objects, messages, and a graphical environment |


## 2. Imperative Programming

In imperative programming, programs consist of a sequence of instructions or commands that modify the state of the program. 

Imperative programs are composed of a series of instructions executed in a specific order. The state of the program, represented by variables, changes as instructions are executed. This paradigm makes extensive use of control structures such as loops and conditional branches. They are often decomposed into subprograms, such as functions and procedures, which support code reuse. Problems are frequently broken down into smaller subproblems that are solved step by step. Each instruction is executed sequentially, modifying the program state until a final state is reached. The goal is to specify explicitly what the computer should do at each step, using assignments, loops, and conditional branches.

| Type | Aspect | Description |
|---|---|---|
| Advantage | Sequential control flow | Based on simple, explicit instructions; often described as intuitive |
| Advantage | Direct control | Provides direct control over program state, memory, and variables |
| Advantage | Flexibility | Allows handling of complex algorithms and varied data structures |
| Limitation | Side effects | The program continuously modifies state, which can introduce errors when state changes unexpectedly |
| Limitation | Parallelization | Difficult due to the strict sequential order of instructions |
| Limitation | Readability | Decreases in large programs, where execution order becomes critical to understanding the code |

Functional programming focuses on the application of pure functions and the avoidance of side effects, rather than on sequential execution and state. Object-oriented programming uses objects and classes to encapsulate state and behavior, with an emphasis on abstraction and modularity.

### 2.1 Variables and functions

Variables are memory locations that store values. Their content can change during program execution.

Functions group instructions under a single name, facilitating code reuse. They can take parameters and return results. Functions used in this way remain part of the imperative paradigm. This differs from functional programming, where the use of mutable state and side effects is minimized and functions are treated as first-class objects, supporting a more modular and mathematical approach.

### 2.2 Side Effects

A side effect occurs when a function modifies the state of the program or its environment beyond returning a value. Examples include modifying global variables, modifying objects or data structures passed as parameters, reading or writing files, and producing output such as printed messages.

For instance, a function that appends an element to a list passed as a parameter modifies that list directly rather than returning a new value. This constitutes a side effect, since the state of the program changes as a result of the function call.

Side effects can make programs more difficult to understand and debug, since they may introduce unforeseen changes to the overall program state. Functions that modify global variables or shared data structures in ways not visible to the caller can produce unexpected results, reducing the predictability of program behavior.

## 3. Declarative Programming

Declarative programming is a paradigm in which the programmer specifies what should be accomplished rather than how to accomplish it. The focus is placed on the logic of the program, and on the rules and relationships between data, leaving the compiler or interpreter to determine how instructions are executed.

In imperative programming, the emphasis is placed on the sequence of operations needed to obtain a result. In declarative programming, the emphasis is placed on describing relationships and outcomes rather than the steps required to achieve them.

### 3.1 Sub-Paradigms of Declarative Programming

Functional programming is based on the evaluation of mathematical functions, without mutable state or side effects. Haskell is cited as an example of a language built around this paradigm.

Logic programming is based on formal logic, principally first-order logic. Prolog is cited as an example of a language built around this paradigm.

In constraint programming, solutions to a problem are obtained by expressing constraints on the possible values of variables, typically using constraint solvers.

## 4. Logic Programming

A logic program is a collection of facts, rules, and queries. Facts define information known to the program. For example, a fact might state that Pierre is the parent of Paul. Rules express conditional relationships between facts: if certain conditions are met, a conclusion is drawn. For example, a rule might state that a person X is the grandparent of Y if X is the parent of Z and Z is the parent of Y. Queries allow the fact and rule base to be interrogated in order to derive information.

Logic programming relies principally on first-order logic. Programs are composed of predicates, and answers are obtained through logical inference.

Three concepts are central to logic programming:

- Unification: the process of matching variables between queries and facts.
- Resolution: an inference method used to derive new information from known rules and facts.
- Backtracking: a technique in which the logic engine tries alternative solutions and reverts to a previous state if an attempt fails.

A simple Prolog program can state that Pierre is a man, Marie is a woman, and that Pierre and Marie are the parents of Paul. Rules can then define what constitutes a father or a mother based on these facts. Queries can subsequently be posed to determine, for instance, whether Pierre is the father of Paul.

| Type | Aspect | Description |
|---|---|---|
| Advantage | Expressiveness | Allows representation of complex relationships and formulation of problems as logical rules |
| Advantage | Clarity | Focuses on the logic of the problem rather than on implementation steps |
| Advantage | Automatic inference | The logic engine derives answers from facts and rules without explicit procedural instructions |
| Limitation | Performance | Backtracking and resolution can introduce performance costs |
| Limitation | Applicability | Mainly used in domains such as expert systems, planning, and formal verification; less suited to problems such as user interface design or high-performance computing |


## 5. Functional Programming

Functional programming is based on pure functions, immutability, and the absence of side effects. The functions map, filter, and reduce are commonly associated with this paradigm, as they operate on collections of data while following these principles.

Although imperative programming also makes use of functions, functional programming is distinguished by its emphasis on function composition, immutability, and the avoidance of side effects.

### 5.1 The `map()` Function

The `map()` function applies a given function to each element of a sequence, such as a list or a tuple, and returns an iterable containing the results. It takes two arguments: the function to apply, and the iterable on which to apply it. The result is a map object, which can be converted into a list or another collection type.

Example: computing the square of each number in a list.

```python
n = [1, 2, 3, 4]
carres = list(map(lambda x: x ** 2, n))
```

Here, each element of `n` is passed to the lambda function `lambda x: x ** 2`, which returns the square of the number.

### 5.2 The `filter()` Function

The `filter()` function is used to select elements of a sequence according to a condition. It applies a test function, returning True or False, to each element of the iterable, and retains only those elements for which the test function returns True. Like `map()`, `filter()` returns an object that can be converted into a list or another collection.

Example: filtering even numbers from a list.

```python
nombres = [1, 2, 3, 4, 5, 6]
pairs = list(filter(lambda x: x % 2 == 0, nombres))
```

### 5.3 The `reduce()` Function

The `reduce()` function, available in the functools module, applies a function cumulatively to the elements of an iterable, from left to right, to produce a single value. It takes a function combining two arguments, an iterable of values, and an optional initial value to start the accumulation.

Example: computing the sum of a list.

```python
from functools import reduce
nombres = [1, 2, 3, 4]
somme = reduce(lambda a, b: a + b, nombres)
```

A combined example illustrates the use of `filter`, `map`, and `reduce` together: filtering a list to keep only even numbers, applying a function to square each remaining number, and reducing the results to obtain the sum of the squares.

```python
from functools import reduce
nombres = [1, 2, 3, 4, 5, 6]
resultat = reduce(lambda a, b: a + b, map(lambda x: x ** 2, filter(lambda x: x % 2 == 0, nombres)))
```

## 6. Comparative Analysis of Paradigms

| Comparison | Paradigm | Description |
|---|---|---|
| Imperative vs. Declarative | Imperative | Specifies the sequence of operations required to reach a result |
| Imperative vs. Declarative | Declarative | Specifies the desired result and the relationships between data, leaving the execution strategy to the compiler or interpreter |
| Functional vs. Imperative | Functional | Avoids mutable state and side effects; relies on pure functions and function composition |
| Functional vs. Imperative | Imperative | Relies on sequential instructions that modify program state directly |






